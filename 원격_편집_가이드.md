# 다른 컴퓨터에서 Cursor로 GitHub 파일 편집하기

## 🚀 빠른 시작 가이드

### 1단계: Git 설치 확인

다른 컴퓨터에서 Git이 설치되어 있는지 확인:
```bash
git --version
```

설치되어 있지 않다면: https://git-scm.com/downloads

### 2단계: GitHub 저장소 클론

터미널 또는 명령 프롬프트에서:
```bash
git clone https://github.com/os-pixel/F1DESK-CONFIG.git
cd F1DESK-CONFIG
```

### 3단계: Cursor에서 프로젝트 열기

1. Cursor 실행
2. `File` → `Open Folder...` (또는 `Ctrl+K Ctrl+O`)
3. 클론한 `F1DESK-CONFIG` 폴더 선택
4. 편집 시작!

## 📝 편집 후 GitHub에 업데이트하기

### 방법 1: Cursor 내장 터미널 사용

1. Cursor에서 `Terminal` → `New Terminal` (또는 `` Ctrl+` ``)
2. 다음 명령어 실행:

```bash
# 변경사항 확인
git status

# 모든 변경사항 추가
git add -A

# 커밋
git commit -m "변경 내용 설명"

# GitHub에 푸시
git push origin main
```

### 방법 2: 자동 커밋 스크립트 사용

프로젝트에 있는 스크립트 사용:

**PowerShell (Windows):**
```powershell
.\auto-commit.ps1 "변경 내용 설명"
```

**배치 파일 (Windows):**
```cmd
auto-commit.bat "변경 내용 설명"
```

## 🔄 다른 컴퓨터에서 최신 버전 가져오기

다른 컴퓨터에서 작업하기 전에 최신 버전을 가져오세요:

```bash
git pull origin main
```

## 💡 팁

### 1. **자동 동기화 설정**
- Cursor의 Git 확장 기능 사용
- 변경사항 자동 감지 및 커밋 제안

### 2. **브랜치 사용 (선택사항)**
```bash
# 새 브랜치 생성
git checkout -b feature/new-feature

# 작업 후 커밋
git add -A
git commit -m "새 기능 추가"

# GitHub에 푸시
git push origin feature/new-feature
```

### 3. **충돌 해결**
다른 컴퓨터에서 작업하다가 충돌이 발생하면:
```bash
# 최신 버전 가져오기
git pull origin main

# 충돌 해결 후
git add -A
git commit -m "충돌 해결"
git push origin main
```

## 🔐 인증 설정

### GitHub Personal Access Token 사용

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. `Generate new token` 클릭
3. 권한 선택: `repo` (전체 권한)
4. 토큰 생성 후 복사
5. Git에 저장:
```bash
git config --global credential.helper store
```
6. 다음 `git push` 시 토큰 입력 (비밀번호 대신)

### SSH 키 사용 (권장)

1. SSH 키 생성:
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

2. 공개 키를 GitHub에 추가:
   - `~/.ssh/id_ed25519.pub` 파일 내용 복사
   - GitHub → Settings → SSH and GPG keys → New SSH key

3. 저장소를 SSH URL로 변경:
```bash
git remote set-url origin git@github.com:os-pixel/F1DESK-CONFIG.git
```

## 📋 체크리스트

다른 컴퓨터에서 작업하기 전:
- [ ] Git 설치 확인
- [ ] GitHub 저장소 클론
- [ ] Cursor에서 프로젝트 열기
- [ ] `git pull origin main`으로 최신 버전 확인
- [ ] 편집 시작!

작업 후:
- [ ] 변경사항 확인 (`git status`)
- [ ] 커밋 및 푸시 (`auto-commit.ps1` 또는 수동)
- [ ] GitHub에서 변경사항 확인

## 🆘 문제 해결

### "Repository not found" 오류
- 저장소 이름 확인: `F1DESK-CONFIG`
- 접근 권한 확인

### "Authentication failed" 오류
- Personal Access Token 또는 SSH 키 설정 확인
- GitHub 인증 정보 확인

### "Your branch is behind" 오류
```bash
git pull origin main
# 충돌이 있으면 해결 후
git push origin main
```

## 📚 추가 리소스

- Git 공식 문서: https://git-scm.com/doc
- GitHub 가이드: https://docs.github.com
- Cursor Git 통합: Cursor 내장 도움말 참조

